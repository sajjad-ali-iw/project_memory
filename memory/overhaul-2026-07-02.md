---
name: overhaul-2026-07-02
description: "Home/About/Contact overhaul deployed 2026-07-02 — services tabs content, branded AI imagery set, dead-link 302 fallbacks, footer hover animation; old image filenames deleted"
metadata:
  node_type: memory
  type: project
---

**Commit `c624ac2` (2026-07-02, deployed to prelive + verified).** Big overhaul of homepage / About / Contact:

- **Homepage services tabs** (patterns/services.php): Accounting/Analytics/ERP-AI tabs now carry full 4-5 item content (ported from staging, humanized). Hero H1 says "AI-powered **Odoo** ERP" again; announcement bar reworded.
- **Imagery replaced repo-wide**: 19 new branded images (AI-generated, purple #50269E family, processed to slot specs from docs/COMPONENTS.md §C). ⚠️ OLD FILENAMES ARE DELETED — `about/overview.png`, `about/cta-pro.jpg`, `odoo-impl/hero.jpg|why.jpg|cost.jpg|hero-ui.png`, `staging/preview-services.jpg|preview-industries.jpg`, `how-it-works/<step>.jpg` (now `how-01…how-08`), `services/accounting-modern.webp|Group-18.webp`, `key-benefits/benefit.png` (was an SAP screenshot — never reference it). If a NEW baked page or menu item needs a preview image, use `staging/menu-preview-services.jpg` / `menu-preview-industries.jpg`.
- **About locations** rebuilt as "One team, three continents" (`.ix-aloc` restyled: stats band + office rows); full NAP lives only on Contact (`#locations` anchor added). Don't re-add address cards to About.
- **404 fallbacks** (functions.php, after the canonical-slug map): unbuilt industry slugs 302 → `/industries/`, `/pricing/` 302 → `/support/` — each redirect only fires while `is_404()`, so publishing the real page auto-disables it. Remove slugs from the list as pages ship (optional; harmless to leave).
- **Footer office cards** (`.ix-foffice`): staging-parity hover animation added (translateY lift + 3px gradient top bar; amber always-on for HQ). Ported from staging's `.iw-loc__card` CSS.
- Copy: seamless/streamline/robust/unlock removed sitewide (client quotes + case-study titles untouched). Mega-menu preview imgs + industries miniImg now get real alts (JS sets them on swap).

**Gotcha:** when adding mega-menu items, don't reference `staging/preview-services.jpg` (deleted) — a 2026-07-02 mega-menu commit did and was fixed in `c624ac2`. Use `menu-preview-services.jpg`.

**Still open (WP-side, not theme):** Site Title "IndexWorld Revamp" leaks into og:title; About page has no og:image; homepage canonical → prelive while About/Contact → indexworld.net; LiteSpeed Purge All needed after each CSS deploy; contact form needs one live test submission. Homepage "About Index World" trio should become real team photos (AI substitutes rejected for E-E-A-T).
